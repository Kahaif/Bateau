import {Component} from '@angular/core';

/**
 * Noop empty component, used to trigger navigation and session restoring
 */
@Component({
  standalone: false,
  templateUrl: ``
})
export class NoopComponent{}
